class seed_crawler_model:
    def submit_query_terms(term_list):
        return urls # results from Bing search
        
    
    def submit_selected_urls(postive, negative):
        # Test new positive and negative examples with exisitng classifier
        # If accuracy above threshold classify pages
        # Ranking 
        # Diversification
        
        return urls # classified, ranked, diversified 

    def extract_terms():
        return terms

    def submit_selected_terms(positive, negative):
        return terms # ranked
